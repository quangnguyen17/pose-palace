/*
export function isFormFilled(
  firstName: string,
  lastName: string,
  email: string,
  phone: string
): boolean {
  return (
    firstName.trim() !== '' &&
    lastName.trim() !== '' &&
    email.trim() !== '' &&
    phone.trim() !== ''
  );
}
*/